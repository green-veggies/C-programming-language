#include<stdio.h>//swapping using 3 variables
int main()
{
    int a,b;
    printf("enter 2 num :");
    scanf("%d %d",&a,&b);
    int temp=a;
    a=b;
    b=temp;
    printf("swapped numbers are :%d %d",a,b);
    return 0;
}